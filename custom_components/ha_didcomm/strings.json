{
  "config": {
    "step": {
      "user": {
        "title": "Connect to ha-didcomm",
        "description": "Enter the gateway URL and owner token.",
        "data": {
          "url": "Gateway URL",
          "owner_api_token": "Owner API token"
        },
        "data_description": {
          "url": "HTTPS URL published by the ha-didcomm TLS proxy.",
          "owner_api_token": "A random token of at least 32 characters configured on the gateway."
        }
      },
      "reconfigure": {
        "title": "Reconfigure ha-didcomm",
        "description": "Update the gateway URL or add or replace its owner token.",
        "data": {
          "url": "Gateway URL",
          "owner_api_token": "Owner API token"
        },
        "data_description": {
          "url": "The integration verifies the gateway before saving the new URL.",
          "owner_api_token": "Existing entries remain read-only until this token is configured. Leave blank to keep the current token."
        }
      }
    },
    "error": {
      "cannot_connect": "Unable to connect to the gateway or its owner API",
      "invalid_auth": "The gateway rejected the owner API token"
    },
    "abort": {
      "already_configured": "This gateway is already configured",
      "reconfigure_successful": "The gateway configuration was updated",
      "wrong_gateway": "The new URL belongs to a different gateway"
    }
  },
  "services": {
    "create_invitation": {
      "name": "Create invitation",
      "description": "Create a single-use DIDComm invitation for a guest agent.",
      "fields": {
        "config_entry_id": {"name": "Gateway", "description": "Gateway that will create the invitation."},
        "label": {"name": "Label", "description": "Optional label shown to the invited agent."}
      }
    },
    "issue_credential": {
      "name": "Issue credential",
      "description": "Issue a scoped, expiring access credential to a connected agent.",
      "fields": {
        "config_entry_id": {"name": "Gateway", "description": "Gateway that will issue the credential."},
        "connection_id": {"name": "Connection ID", "description": "DIDComm connection receiving the credential."},
        "subject_did": {"name": "Subject DID", "description": "Holder DID that will be named as the credential subject."},
        "permissions": {"name": "Permissions", "description": "Home Assistant entity ID patterns this credential permits."},
        "role": {"name": "Role", "description": "Role recorded in the credential."},
        "duration_hours": {"name": "Duration", "description": "Number of hours before the credential expires."}
      }
    },
    "revoke_credential": {
      "name": "Revoke credential",
      "description": "Immediately revoke one issued access credential.",
      "fields": {
        "config_entry_id": {"name": "Gateway", "description": "Gateway that issued the credential."},
        "credential_exchange_id": {"name": "Credential exchange ID", "description": "Identifier of the credential to revoke."}
      }
    },
    "revoke_connection": {
      "name": "Revoke connection",
      "description": "Immediately revoke every credential issued for a connection.",
      "fields": {
        "config_entry_id": {"name": "Gateway", "description": "Gateway that owns the connection."},
        "connection_id": {"name": "Connection ID", "description": "Connection whose credentials will all be revoked."}
      }
    }
  },
  "exceptions": {
    "entry_unavailable": {"message": "The selected ha-didcomm gateway is not loaded."},
    "owner_not_configured": {"message": "Owner controls are not configured. Reconfigure the integration with an owner token."},
    "invalid_owner_token": {"message": "The gateway rejected the configured owner token."},
    "record_not_found": {"message": "The requested credential or connection was not found."},
    "invalid_permissions": {"message": "At least one non-empty permission is required."},
    "owner_operation_failed": {"message": "The gateway could not complete the owner operation."}
  },
  "entity": {
    "sensor": {
      "credential": {
        "name": "Credential {credential_id}",
        "state": {
          "active": "Active",
          "expired": "Expired",
          "revoked": "Revoked",
          "invalid": "Invalid"
        }
      }
    }
  }
}
